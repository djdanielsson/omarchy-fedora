[Unit]
Description=Refresh system timezone from location periodically

[Timer]
# Shortly after boot (travel = new network = new zone), then twice daily.
OnBootSec=2min
OnUnitActiveSec=12h
Persistent=true

[Install]
WantedBy=timers.target
