# Omarchy-Fedora cursor defaults — ensures a visible pointer in
# SDDM / Hyprland / XWayland even before Hyprland env{} is parsed.
# Requires adwaita-cursor-theme (see build_files/packages-common.txt).
export XCURSOR_THEME=Adwaita
export XCURSOR_SIZE=24
export HYPRCURSOR_THEME=Adwaita
export HYPRCURSOR_SIZE=24
