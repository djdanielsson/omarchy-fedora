# zram swap — BIB autopart creates no swap; without this T14s OOMs and
# suspend/hibernate misbehaves. 50% RAM up to 8G, zstd, high priority.
[zram0]
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
